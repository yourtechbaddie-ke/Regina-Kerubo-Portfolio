const header=document.querySelector('header');window.addEventListener('scroll',()=>header.classList.toggle('scrolled',scrollY>30));const cards=document.querySelectorAll('.code-wall,.portrait-frame');const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;if(!reduce){window.addEventListener('pointermove',e=>{const x=(e.clientX/innerWidth-.5)*2,y=(e.clientY/innerHeight-.5)*2;cards.forEach((c,i)=>{c.style.transform=`translate(${x*(i?4:2)}px,${y*(i?3:2)}px) rotate(${i?3+x*1.2:4+x*.7}deg)`})})}