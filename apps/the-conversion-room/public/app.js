const fmt=n=>new Intl.NumberFormat('en-US').format(n);document.querySelectorAll('[data-count]').forEach(el=>{const target=Number(el.dataset.count);let start=0;const step=target/55;const tick=()=>{start=Math.min(target,start+step);el.textContent=fmt(Math.round(start));if(start<target)requestAnimationFrame(tick)};const io=new IntersectionObserver(es=>{if(es[0].isIntersecting){tick();io.disconnect()}},{threshold:.5});io.observe(el)});const btn=document.getElementById('diagnose'),out=document.getElementById('diagnosis');btn?.addEventListener('click',()=>{out.classList.remove('hidden');out.innerHTML='<strong>DIAGNOSIS / FRICTION AFTER INTENT</strong><p>Priority: reduce the distance between qualified interest and a confident next step. Test stronger proof beside the primary CTA, shorten the enquiry path, and give high-intent responders a human follow-up window. In this simulation, improving the intent → action rate from 17% to 22% would create roughly 320 additional actions without buying additional reach.</p>';btn.textContent='Diagnosis complete ↗';btn.disabled=true});document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const t=document.querySelector(a.getAttribute('href'));if(t){e.preventDefault();t.scrollIntoView({behavior:'smooth'})}}));