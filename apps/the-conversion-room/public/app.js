const fmt=n=>new Intl.NumberFormat('en-US').format(n);document.querySelectorAll('[data-count]').forEach(el=>{const target=Number(el.dataset.count);let start=0;const step=target/70;const tick=()=>{start=Math.min(target,start+step);el.textContent=fmt(Math.round(start));if(start<target)requestAnimationFrame(tick)};const io=new IntersectionObserver(es=>{if(es[0].isIntersecting){tick();io.disconnect()}},{threshold:.35});io.observe(el)});const btn=document.getElementById('diagnose'),out=document.getElementById('diagnosis');btn?.addEventListener('click',()=>{out.classList.remove('hidden');out.innerHTML='<strong>DIAGNOSIS / BRIDGE FRICTION</strong><p>The room would not buy more reach first. It would strengthen the intent → action bridge: move proof closer to the primary CTA, simplify the enquiry path, state the response expectation, and route high-intent visitors toward contextual human follow-up. At a 17% intent-to-action rate, 6,412 qualified people produce roughly 1,090 actions. At the 22% test target, the same audience produces roughly 1,411 actions — about 320 additional actions without purchasing additional reach.</p>';btn.textContent='Diagnosis complete ↗';btn.disabled=true});document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const t=document.querySelector(a.getAttribute('href'));if(t){e.preventDefault();t.scrollIntoView({behavior:'smooth'})}}));